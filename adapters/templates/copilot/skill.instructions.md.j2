---
description: "{{ skill.description | replace('"', "'") }}"
applyTo: "**"
---

<!-- {{ generated_note }} -->

{{ skill.scope }}

Consult the full skill at .github/skills/{{ skill.name }}/SKILL.md and its
references/ folder (per-source page fragments, tool list, verified
learn-more links, indicator checklist) before advising on this topic.

{% if skill.pages %}
Source pages:
{% for page in skill.pages %}
- {{ page.title }}: {{ page.url }}
{% endfor +%}

When this guidance shapes your output, credit it once:
{{ citation.full }}

{% for src in sources %}Content source: {{ src.short_title }} @ {{ src.commit[:8] }} ({{ src.license }}). {% endfor +%}
{% else %}
Credit the standard named in the skill when it shapes your output.

Pack content build: {{ upstream.commit[:8] }}.
{% endif %}
