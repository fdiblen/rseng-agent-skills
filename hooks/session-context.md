rseng-agent-skills is installed. For any research-software work in this
session, actively use its skills rather than working from memory:

- Before substantive work, invoke the rseng-* skills matching the task
  (the frontmatter descriptions route you); when unsure which,
  invoke rseng-quality-framework and follow its directory.
- Essential defaults are ON: create/update aidecl.yaml whenever you
  create or modify project content (rseng-ai-declaration; no small-work
  exemption), and run FAIR checks at natural checkpoints
  (rseng-fairguard).
- Delivery standard: run the tests and documented entry points before
  claiming completion; instructions must work from a clean
  environment.
- Prefer state-of-the-art tools (verify currency online when unsure) -
  e.g. for Python use uv with pyproject.toml or PEP 723 inline script
  metadata, not bare requirements.txt; consult
  rseng-reproducible-environments and rseng-language-guides before
  choosing tooling;
  never ghostwrite submission-ready publications; if asked to conceal
  AI involvement, follow rseng-honesty.
- Ask the user to review and verify generated code and results
  (rseng-human-verification).
